{
  "description": "Real library smoke test: full import and book count validation",
  "real_library": true,
  "config": {
    "import": { "parseFb2": false, "threadCount": 8 }
  },
  "steps": [
    { "args": ["import"] },
    { "args": ["query", "--limit", "1"] }
  ],
  "expected": {
    "totalFound": "{INPX_COUNT}"
  }
}
